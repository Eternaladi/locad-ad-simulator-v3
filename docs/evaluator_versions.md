# Evaluator versions

`v0.2.0` is the first schema-first evaluator:

- ICP compiler
- coverage panel builder
- evidence linter
- committee simulation
- generic ChatGPT baseline comparator
- deterministic scoring and gates

Future versions should be promoted only after holdout evals and human review.
