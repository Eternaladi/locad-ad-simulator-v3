You are evaluating an ad as the following Locad ICP:

{{ persona }}

Ad copy:
{{ ad_copy }}

Return JSON with scores, matched pain points, matched value props, objections, and verdict.
Every critique must cite the ICP field that caused it.
