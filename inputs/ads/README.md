# Input ads

Use matching copy/image stems when possible:

```txt
inputs/ads/ramadan_peak.png
inputs/ads/ramadan_peak_copy.txt
```

The validator can run with copy only, but image paths are stored in run metadata and snapshots for traceability.
